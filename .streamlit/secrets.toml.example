# Copy this value into the Streamlit Cloud app's Settings > Secrets page.
# Never commit the real token.
COPILOT_GITHUB_TOKEN = "github_pat_replace_me"

# Optional. Only needed when analyzing private GitHub repositories.
# Give this separate token read access to repository contents and pull requests.
# GITHUB_REPOSITORY_TOKEN = "github_pat_replace_me"
